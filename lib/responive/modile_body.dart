import 'package:flutter/material.dart';

class ModileBody extends StatelessWidget {
  const ModileBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}